# DRIFT 3D — Reuse-First Asset and Systems Matrix

**Status:** `GOVERNANCE RECONCILIATION RECORD` — inventories what exists today and what a future acquisition lot would need to evaluate. Reconciled under `DRIFT-IV-GOV-40`; status taxonomy and evidence-honesty audit reconciled under `DRIFT-IV-GOV-40`'s authorial reconciliation correction. **`DRIFT-IV-PRE-20` (2026-08-03) is the first lot to actually perform that dedicated acquisition/verification work** — see §0's updated note, the new rows in §1/§2, and the corrections to §3, below.
**`GOV-40`'s own original claim stands unchanged for everything not touched by `DRIFT-IV-PRE-20`:** no asset download occurred to produce the *original* version of this document. `DRIFT-IV-PRE-20` is the first lot that actually fetched, hashed, technically inspected and visually evaluated real files — 5 real downloads, all quarantined outside the tracked repository under `.tmp/drift-pre-20/`, full record at `docs/evidence/DRIFT-IV-PRE-20/licensed-asset-provenance-registry.md`. **No row in this matrix claims an asset is reusable without that verification actually having happened** — rows not touched by `DRIFT-IV-PRE-20` remain exactly as honest about their own `REFERENCE` status as `GOV-40` originally left them.

**Status taxonomy (this correction):** `ADOPT` (confirmed, in-repo or licence-verified, usable now or with trivial integration) · `PILOT` (real and working, but needs rework/generalization before it can serve more than its current one-off track) · `REFERENCE` (a named category or pattern, not a verified specific asset — evaluation only, not yet actionable) · `REJECT` (explicitly not to be adopted, with reason). This taxonomy replaces the prior pass's `REUSE-AS-IS`/`ADAPT`/`REFERENCE-ONLY`/`REJECT`/`BUILD-NEW` vocabulary — `REUSE-AS-IS`→`ADOPT`, `ADAPT`→`PILOT`, `REFERENCE-ONLY`→`REFERENCE`, `REJECT`→`REJECT` (unchanged), `BUILD-NEW` retired (it was never actually assigned to any row in the prior pass).

Columns (this correction, full set per the GOV-40 audit requirement): **Exact project/source** · **Exact reusable element** · **Licence & provenance** · **Maintenance/activity** · **Format & dependencies** · **R3F compatibility** · **Technical adaptation cost** · **Beneficiary eras/tracks** · **Status**.

---

## 0. Honest evidence audit — what prior research actually exists (read before using this matrix)

**The owner's instruction for this correction, verbatim intent: "Do not replace missing prior research with generic marketplace suggestions. Mark missing evidence honestly."** This section states plainly what was actually found.

A repository-wide search (this correction, `grep -rniE` across all of `docs/`, excluding this lot's own authored GOV-40 files, for: `sketchfab`, `github\.com/<owner>/<repo>`-style references, `mixamo`, `quaternius`, `kenney`, `turbosquid`, `cgtrader`, `readyplayer\.me`, `three\.js examples`/`threejs\.org`, `polyhaven\.com`) returned **zero hits**. No prior project conversation, decision log entry, or governance document names a specific Three.js example, GitHub repository, or third-party model/asset source anywhere in this project's history, outside of what this document itself proposes.

**The one real, confirmed, previously-researched exception:** the 5 Poly Haven CC0 photo material sets already in `public/textures/`, documented in `docs/DECISIONS_LOG.md`'s "24 — Photo-textures CC0" entry and cited by name in `DRIFT_3D_REALISM_BIBLE.md`. This is genuine prior research with a real, verifiable source — carried into §1 below as `ADOPT`.

**Everything else in this document's §3 (candidate categories: Mixamo-style rigs, Kenney.nl, Quaternius, additional Poly Haven sets, generic CC0 prop packs) is this lot's own first-time suggestion, authored during `DRIFT-IV-GOV-40`, never a recovery of prior project research.** `docs/DECISIONS_LOG.md`'s own "restes" (leftover-work) notes about future asset needs — e.g. *"modèles CC retravaillés (véhicule, passeur, mobilier urbain) et foule animée par vertex"*, *"KTX2/compression et roughness maps si le poids devient un sujet"* — confirm that acquiring such assets was always an intended future step, but **name no specific source, pack, repository, or file** for any of it. Treating a category name as if it were vetted prior research would misrepresent this document's own evidentiary basis; every §3 row below is marked `REFERENCE` and flagged individually to prevent that misreading.

**`DRIFT-IV-PRE-20` addendum (2026-08-03) — this is the dedicated acquisition lot §0 above calls for:** primary-source licence research (official pages/repos/terms only, no blog/forum/aggregator summaries), 5 real downloads (SHA-256-hashed, quarantined under untracked `.tmp/drift-pre-20/`, never committed as raw binaries), real GLB/glTF structural parsing (mesh/material/node/animation/skin/texture counts, extension usage), and real visual evaluation against the 5 owner-accepted `DRIFT-IV-PRE-10` masterframes. Full method, every finding, and every source citation: `docs/evidence/DRIFT-IV-PRE-20/licensed-asset-provenance-registry.md`. Results: 1 candidate promoted to `ADOPT` (Poly Haven `snow_02`), 4 promoted to `PILOT` (Kenney Mini Characters, Kenney City Kit Commercial, Kenney Car Kit, three.js `Water.js`/`Sky.js`), 2 `REJECT`ed with reasons (Mixamo — licence terms; Kenney Nature Kit — Realism Bible PBR conflict), the rest remaining honest `REFERENCE` (Quaternius — licence confirmed but its Google Drive-hosted binary download endpoint specifically was blocked this session on 2 tested packs; its own official website remained fully accessible throughout, see the precise account in the evidence file §14.1; a Machine/Prop pack and further Poly Haven material sets — genuinely not yet evaluated). **Owner decision, this same lot:** the 5-candidate promoted set is accepted, each with an explicit bounded `PILOT`/`ADOPT` role (evidence file §14.2) — `DRIFT-IV-PRE-20 = DONE_PENDING_MERGE`. See §1–§3 below for the exact updated rows.

**`DRIFT-IV-PRE-30` addendum (2026-08-03) — the four `PILOT`-status candidates above were actually loaded and run:** `DRIFT-IV-PRE-30` built one real pilot per candidate (Kenney Mini Characters, Kenney City Kit Commercial, Kenney Car Kit, three.js `Water.js`/`Sky.js`), plus wired the `ADOPT`-status `snow_02` set into a real material for the first time, at an internal `/drift-kit-lab` route — no row's `Status` column changes (`PILOT`/`ADOPT` still means licence-verified-and-buildable, not yet final-art-accepted). **Owner live review, this same lot:** the owner personally ran all three pilots and issued three `ACCEPT` verdicts, each bounded to technical/architectural scope only, plus an explicit formal guardrail — `TECHNICALLY ACCEPTED — VISUAL REALISM NOT ACCEPTED AS FINAL ART` (verbatim: *"severely lacking in realistic graphics"*). This does **not** upgrade any Kenney-sourced row above to a final-art status; it confirms the `PILOT` mechanism itself is viable while leaving every row's own visual-register caveat exactly as recorded. Full record: `docs/evidence/DRIFT-IV-PRE-30/shared-kit-pilots-evidence.md` §12.

---

## 1. Confirmed existing, already in the repository (`ADOPT`)

| Exact project/source | Exact reusable element | Licence & provenance | Maintenance/activity | Format & dependencies | R3F compatibility | Technical adaptation cost | Beneficiary eras/tracks | Status |
|---|---|---|---|---|---|---|---|---|
| Poly Haven (polyhaven.com) | 5 photo material sets: `aerial_beach_01`, `brown_planks_07`, `concrete_wall_008`, `red_brick_03`, `rock_boulder_dry` | CC0-1.0, confirmed — Poly Haven's own stated licence for its full catalogue; provenance confirmed present in `public/textures/`, 10 JPG diffuse+normal-GL pairs, cited by name in `DRIFT_3D_REALISM_BIBLE.md`. **`DRIFT-IV-PRE-20` correction:** these 5 sets previously had no formal licence-evidence record anywhere in the repository (only a code-comment citation in `drift3dTextureFactory.ts`) — this gap is now closed retroactively; see `docs/evidence/DRIFT-IV-PRE-20/licences/polyhaven-terms-note.md` for the exact retrieval record (`https://polyhaven.com/license`, retrieved 2026-08-03). No artistic or integration change; factual evidence closure only. | Poly Haven is an actively maintained, ongoing CC0 asset project (external; not this repo's own maintenance burden) | 1K JPG diffuse + normal, no external dependency | Native — plain `Texture`/`MeshStandardMaterial` inputs, no loader beyond `TextureLoader` | Zero — already integrated | All eras (Lighting/Material Kit, world-wide) | `ADOPT` |
| Poly Haven (polyhaven.com) | `snow_02` photo material set (diffuse + normal-GL + roughness, 1K JPG) — author Rob Tuytel | CC0-1.0, confirmed same source/terms as the row above; downloaded and hashed this lot — `snow_02_diff_1k.jpg` SHA-256 `523A4E69C90B96D787DD69B897F29A2B3761A017024405C60AA630D1E8E9009A`; full record in `docs/evidence/DRIFT-IV-PRE-20/licensed-asset-provenance-registry.md` §6.5 (registry id `PRE20-C01`) | Same as above | 1K JPG diffuse + normal-GL + roughness (a genuine roughness channel the existing 5 sets do not ship); confirmed 1024×1024 via direct decode | Native, identical integration path to the existing 5 sets | Zero — same integration path, not yet wired into `drift3dTextureFactory.ts` | Older Shadows (Rise's snow-line, mountain plateau masterframe) | `ADOPT` (`DRIFT-IV-PRE-20`, not yet integrated into the runtime — that remains a `PRE-30`/Build-lot action) |
| In-repo original code | Terrain heightfield engine (`drift3dTerrain.ts`) | N/A — original code authored in this repository | This repo's own codebase | Procedural (analytical function, not a file asset), zero external dependencies | Native — authored directly against R3F/`three` | Zero — already the Terrain/Road Kit as-is | All eras | `ADOPT` |
| In-repo original code | Vegetation/prop scatter archetypes (conifer, broadleaf, bush, rock, grass, deadTree, lamppost, acacia, poppy, cityBlock) (`drift3dScatter.ts`) | N/A — original code | This repo's own codebase | Procedural, `InstancedMesh`, zero external dependencies | Native | Zero — already instanced, ~12 draw calls | All eras (Vegetation Kit) | `ADOPT` |
| In-repo original code | Region lighting/color-script state (`drift3dAtmosphere.ts`) | N/A — original code | This repo's own codebase | Procedural, zero external dependencies | Native | Zero | All eras | `ADOPT` |
| In-repo original code | Diegetic ambience (6 synthesized WebAudio layers) (`drift3dAmbience.ts`) | N/A — original code | This repo's own codebase | Synthesized, no audio files, zero external dependencies | Native (WebAudio, not R3F, but zero-conflict) | Zero | All eras | `ADOPT` |
| Owner's own catalogue | 27 track/ambient MP3s | Owner-owned | Owner's own catalogue | MP3, N/A (audio, not 3D) | N/A | Zero | All eras | `ADOPT` (out of this matrix's 3D-asset scope, listed for completeness) |
| In-repo original code, debugged through this repo's own P0–P0I forensic arc | EUX GAINENT dynamic screen pipeline (`CanvasTexture`-per-state pattern) | N/A — original code | This repo's own codebase; delivered on `main` via PR #32 at commit `b069d09` (`DRIFT-IV-BY-EUX-30`, owner-accepted) — see `docs/DRIFT_3D_RUNTIME_MIGRATION_MAP.md` §0/§4, classified `DONE — MERGED` (historical branch-head `drift-iv-by-eux-30-owner-acceptance` at `c5ca4da847e2dab24f39b50025384f80fe6ca857`, superseded by the merge) | Procedural canvas texture, zero external dependencies | Native | Low as a pattern (`ADOPT`); Medium to extract into a parametrized shared function (`PILOT`, see §2) | Birth Yard (Signage/Screen Kit seed) | `ADOPT` (the pattern itself, owner-accepted and merged; extraction into a shared kit function still pending) |

---

## 2. Confirmed existing, but not reusable as-is (`PILOT` or `REJECT`)

| Exact project/source | Exact reusable element | Licence & provenance | Maintenance/activity | Format & dependencies | R3F compatibility | Technical adaptation cost | Beneficiary eras/tracks | Status |
|---|---|---|---|---|---|---|---|---|
| In-repo original code | EUX GAINENT athletes (cylinder+sphere / extracted-but-bespoke body build) | N/A — original code; two divergent versions across `main` and the unmerged branch (see migration map §0) | This repo's own codebase | Procedural primitives, no skeleton, no clips, zero external dependencies | Native | Medium-High — needs a real shared skeleton before any other track can adopt the same build | Birth Yard only (currently) | `PILOT` — real geometry, not yet shared-ready |
| In-repo original code | EUX GAINENT station/machine archetypes | N/A — original code | This repo's own codebase | Procedural primitives, simple imperative moving-part transform | Native | Low — parametrize into the Machine/Prop Kit | Birth Yard only | `PILOT` |
| In-repo original code (`Drift3DEffects.tsx`) | `FoolfouleCrowd` instanced marching figures | N/A — original code | This repo's own codebase | Procedural, `InstancedMesh`, no skeleton/clips | Native | Medium — decouple from the hardcoded node before it can serve any other track's crowd need | Birth Yard (`foolfoule`) only | `PILOT` |
| In-repo original code (`Drift3DEffects.tsx`) | `StormRain` | N/A — original code | This repo's own codebase | Procedural particle/shader effect | Native | Low-Medium — parametrize into the Weather Kit | New Signal (`hold-the-light`) only | `PILOT` |
| In-repo original code (`Drift3DEffects.tsx`) | `FloatingParticles` | N/A — original code | This repo's own codebase | Procedural particle effect | Native | Low — closest to a proven two-scene pattern already, lowest-cost generalization candidate | Older Shadows (`ethnic-stick`) + New Signal (`midnight-work`) | `PILOT` |
| In-repo original code (`drift3dLandmarks.ts` facade primitive) | EUX GAINENT opaque-glass storefront material | N/A — original code; root cause of the P0-series screen-visibility investigation | This repo's own codebase | Procedural material (no texture/transparency), zero external dependencies | Native | High — needs a genuine translucent-material redesign, not a parameter tweak, without breaking the P0 arc's depth-occlusion lesson | Birth Yard only | `REJECT` (as a general glass-treatment pattern for other tracks to copy verbatim) |
| `three/examples/jsm/objects/Reflector.js` (bundled with the `three` npm package, not a separate asset) | Bare `Reflector`-flag water handling | MIT (same as the `three` package) | Maintained upstream as part of the `three.js` project itself (external to this repo) | Three.js addon class, depends only on `three` (already a project dependency) | Native | Medium — needs the Water Kit's wave/erasure extension before it can serve Étééaooété's ocean | Birth Yard (canal) only, today | `PILOT` — real mechanism, not yet sufficient for the ocean's requirements |
| `three/examples/jsm/objects/Water.js` + `Sky.js` (bundled with the `three` npm package, already installed at `0.185.0`, not a separate asset) | Real-time reflective water shader (`Water` extends `Mesh`, `WebGLRenderer`-only) + atmospheric-scattering sky shader | MIT, confirmed this lot from `node_modules/three/LICENSE`, preserved at `docs/evidence/DRIFT-IV-PRE-20/licences/threejs-LICENSE.txt` | Maintained upstream as part of the `three.js` project itself | Three.js addon classes, zero new npm dependency (already installed) | Native, `Water extends Mesh` | Medium — supplies the exact "wave/erasure extension" the `Reflector.js` row above already named as missing; wave-parameter tuning and Étééaooété's own erasure mechanic remain real integration work | Birth Yard (canal, upgrading the bare `Reflector` flag), New Signal (Étééaooété's ocean) | `PILOT` (`DRIFT-IV-PRE-20`, formally verified — full record in `docs/evidence/DRIFT-IV-PRE-20/licensed-asset-provenance-registry.md` §6.6, registry id `PRE20-C02`) |
| Kenney (kenney.nl) — "Mini Characters" | Rigged generic people, 12 character variants + 10 accessibility props, 7-joint skeleton, 32 named animation clips (walk/idle/sit/interact/emote/wheelchair-*) | CC0-1.0, confirmed this lot from the pack's own official page and its own bundled `License.txt`, preserved at `docs/evidence/DRIFT-IV-PRE-20/licences/kenney-mini-characters-License.txt` | Actively maintained (Kenney, external) | GLB (glTF 2.0), 246,916 B representative file, `KHR_texture_transform` only, no required extensions | Native — `GLTFLoader` compatible, no unsupported extensions | Medium — low-poly flat-shaded visual register mismatches the accepted masterframes' PBR realism at foreground distance; plausible at silhouette/background distance without rework; 7-joint skeleton closely matches the Shared Kit Architecture's own §1.4 minimal-humanoid convention | Birth Yard (bridge queue, Foolfoule crowd density), background human presence world-wide | `PILOT` (`DRIFT-IV-PRE-20`, formally verified — registry id `PRE20-A01`) |
| Kenney (kenney.nl) — "City Kit (Commercial)" v2.1 | Modular buildings/towers/street-furniture detail props, 50 files | CC0-1.0, confirmed this lot, preserved at `docs/evidence/DRIFT-IV-PRE-20/licences/kenney-city-kit-commercial-License.txt` | Actively maintained | GLB (glTF 2.0), 108,936 B representative file, single shared 512×512 RGBA colormap texture, `KHR_texture_transform` only | Native | Medium — same low-poly/flat-shaded mismatch at foreground distance; credible as background/distant massing only | Birth Yard (background tower massing), New Signal (Le Monde S'endort skyline) | `PILOT` (`DRIFT-IV-PRE-20` — registry id `PRE20-A02`) |
| Kenney (kenney.nl) — "Car Kit" v3.1 | Background traffic vehicles (sedan/SUV/van/truck/taxi/police/ambulance/firetruck/tractor/karts), 45 files, separate wheel nodes per vehicle | CC0-1.0, confirmed this lot, preserved at `docs/evidence/DRIFT-IV-PRE-20/licences/kenney-car-kit-License.txt` | Actively maintained | GLB (glTF 2.0), 172,216 B representative file (`sedan.glb`), 5 nodes (body + 4 named wheel nodes), `KHR_texture_transform` only, no skeleton/animation | Native | Low — separate wheel nodes make procedural Y-axis wheel rotation trivial with no rig needed; background-only use avoids the visual-register mismatch | Birth Yard (canal-side traffic), background transit world-wide | `PILOT` (`DRIFT-IV-PRE-20` — registry id `PRE20-B01`) |
| Kenney (kenney.nl) — "Nature Kit" v1.0 | Rocks/trees/foliage/bridges, 329 files | CC0-1.0, confirmed this lot, preserved at `docs/evidence/DRIFT-IV-PRE-20/licences/kenney-nature-kit-License.txt` — **licence is clean; rejection below is technical/artistic, not a licence finding** | Actively maintained | GLB (glTF 2.0), representative files inspected (`rock_tallA.glb`, `tree_default.glb`) confirmed **`KHR_materials_unlit` with zero image/texture references on every mesh** | Native (loads fine), but **produces flat/untextured shading** | High — a full re-texturing pass would be required to meet `DRIFT_3D_REALISM_BIBLE.md`'s non-negotiable "no mesh ships without a textured PBR material" rule; judged disproportionate | n/a — not promoted | `REJECT` (`DRIFT-IV-PRE-20` — registry id `PRE20-B02`; the existing in-repo Vegetation/Terrain `ADOPT` system in §1 above already serves this need, no external acquisition necessary) |

---

## 3. Not yet present — candidate source categories, `REFERENCE`

**None of the items below have been downloaded, inspected, or licence-verified, and none is recovered prior project research (see §0) — each is this lot's own first-time category suggestion.** Each is named because the category is a well-known, generally-CC0/free ecosystem already consistent with this project's Poly Haven precedent. **A future acquisition lot must independently confirm licence terms and provenance for the exact file used before any status here can become `ADOPT` or `PILOT`.**

| Exact project/source | Exact reusable element | Licence & provenance | Maintenance/activity | Format & dependencies | R3F compatibility | Technical adaptation cost | Beneficiary eras/tracks | Status |
|---|---|---|---|---|---|---|---|---|
| Category only — e.g. Mixamo (Adobe) auto-rigged humanoids, or CC0 low-poly character packs such as Quaternius | Rigged humanoid base mesh + walk/idle/wait/gesture clip set (Human/Crowd Kit, Animation Kit) | **`DRIFT-IV-PRE-20` correction:** this category is now partially resolved. Mixamo evaluated and `REJECT`ed for repository-bundled use — Adobe's General Terms of Use §3.6 forbids stand-alone redistribution of raw Content Files outside an embedded End Use (see `docs/evidence/DRIFT-IV-PRE-20/licences/mixamo-adobe-terms-note.md`). Quaternius's `CC0-1.0` licence is confirmed, but this session could not download any file from its official Google Drive distribution (a reproducible shared-folder quota limit, not a licence problem) — remains `REFERENCE`. **Kenney's "Mini Characters" pack was evaluated instead, real rig confirmed (7 joints) with 32 real animation clips, `CC0-1.0`, and is now promoted to `PILOT` — see §2 above.** | Kenney: actively maintained (external). Quaternius: unverified this lot (access blocked) | Kenney: GLB (glTF 2.0), confirmed, see §2. Quaternius: unconfirmed, access blocked | Kenney: native, confirmed compatible, see §2 | Kenney: Medium (visual-register harmonization), see §2 | All eras (Birth Yard density, Ethnic Stick's warm crowd, the twelve silent archetypes once that list exists) | `REFERENCE` retained for Mixamo (`REJECT`ed, kept here as the category's own historical record) and Quaternius (`REFERENCE`, access-blocked); **superseded by `PILOT` for the Kenney candidate, see §2** |
| Category only — e.g. Kenney.nl city/building packs | Building/façade glTF library (Urban Kit) | **`DRIFT-IV-PRE-20` correction:** Kenney's "City Kit (Commercial)" v2.1 evaluated, `CC0-1.0` confirmed, real GLB structure confirmed — promoted to `PILOT`, see §2 above. | Actively maintained | GLB (glTF 2.0), confirmed, see §2 | Native, confirmed, see §2 | Medium (background/distant-massing use recommended), see §2 | Birth Yard primarily, New Signal's skyline | **Superseded by `PILOT`, see §2 above** — this row kept only as the category's own historical record |
| Category only — CC0 low-poly vehicle packs (no specific source named) | Vehicle/traffic background models (Vehicle/Traffic Kit) | **`DRIFT-IV-PRE-20` correction:** Kenney's "Car Kit" v3.1 evaluated, `CC0-1.0` confirmed, separate wheel nodes confirmed (rotation-ready, no rig needed) — promoted to `PILOT`, see §2 above. | Actively maintained | GLB (glTF 2.0), confirmed, see §2 | Confirmed simple wheel-rotation rig (named wheel nodes), see §2 | Low, see §2 | Birth Yard (canal traffic), background transit world-wide | **Superseded by `PILOT`, see §2 above** — this row kept only as the category's own historical record |
| Poly Haven (polyhaven.com) — same source as §1's 5 confirmed sets | Additional PBR material sets beyond the existing 5 (Lighting/Material Kit) | **`DRIFT-IV-PRE-20` correction:** one specific new file (`snow_02`, author Rob Tuytel) evaluated, downloaded, hashed and confirmed `CC0-1.0` — promoted to `ADOPT`, see §1 above. The general category (further sets — ice, sand-beyond-existing, machined metal, and a Mountain Kit rock/cliff dressing set) remains open for a future lot to repeat this exact precedent. | Actively maintained (external, confirmed for the project generally) | JPG diffuse+normal+roughness, convertible to KTX2 | Native (matches §1's already-integrated pattern) | Low — same integration path as the existing 5 | All eras, as new material needs arise (ice, sand-beyond-existing, machined metal, Mountain Kit rock/cliff dressing) | `REFERENCE` retained for the general "further sets" category; **`snow_02` specifically superseded by `ADOPT`, see §1 above** |
| Category only — e.g. Kenney.nl, Quaternius prop packs (no specific source named) | Mechanical/prop detail models beyond current primitive assemblies (Machine/Prop Kit) | **Unverified — not deep-evaluated by `DRIFT-IV-PRE-20`.** Kenney's "City Kit (Commercial)" (see §2 above, promoted `PILOT`) does include some street-furniture detail props (awnings, overhangs, parasols) as a side effect of its own urban evaluation, but no dedicated Machine/Prop pack was independently sourced or evaluated this lot. | **Unverified** | glTF expected, no specific file confirmed | Simple part-hierarchy for moving parts, unconfirmed | **Unverified** | Vegetative Field's domestic machines, New Signal's cranes/interchange props | `REFERENCE` — category only, genuinely still not recovered/actionable research |

---

## 4. Explicitly rejected candidate approaches

| Approach | Why rejected |
|---|---|
| Any asset without a recorded licence/provenance, adopted anyway | Violates this lot's own constraint and `DRIFT_3D_INTEGRAL_SYSTEMS_ARCHITECTURE.md` §8.2's existing "documenter licence et provenance" rule — no exception. |
| Downloading assets speculatively during this documentation-only lot | Explicitly forbidden by this lot's own constraints ("No asset downloads"). |
| Treating a Mixamo-rigged character as automatically reusable | Mixamo's terms are not CC0 and require explicit case-by-case review — never assume compatibility; a future acquisition lot must read Adobe's actual current terms before any adoption decision. |
| Treating this document's own §3 category suggestions as if they were previously vetted, sourced research | This is exactly the honesty failure this correction exists to prevent — §3 is first-time authorship by this lot, not a recovery of prior project work (see §0). |
| Building bespoke, non-shared assets for each of the remaining 25 tracks, one at a time | This is exactly the pattern `DRIFT_3D_RUNTIME_MIGRATION_MAP.md` documents as the current state for humans/crowds/weather — the reason this lot exists is to stop this pattern from compounding, not to continue it under a new name. |

---

## 5. What this matrix authorizes

Nothing by itself. Per `DRIFT_3D_INTEGRAL_SYSTEMS_ARCHITECTURE.md`'s own standing rule and this lot's own constraints, no kit is built and no asset is acquired until the resequenced `DRIFT-IV-PRE-*` gates in `docs/DRIFT_3D_INTEGRAL_BACKLOG.md` are reached and separately executed. This document exists so that when that lot opens, the candidate list above is the starting point for real evaluation — not a blank page, and not a false claim of readiness. §0's honest evidence audit stands as this document's own record that its §3 candidates are proposals, not findings.
